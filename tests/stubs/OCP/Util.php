<?php

declare(strict_types=1);

namespace OCP;

class Util {
    public static function connectHook(string $signalClass, string $signalName, $slot, string $slotName): void {
    }
    
    public static function emitHook(string $signalClass, string $signalName, array $params = []): void {
    }
}